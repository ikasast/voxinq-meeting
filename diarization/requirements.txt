# Speaker diarization, on sherpa-onnx (ONNX Runtime).
#
#   pip install -r requirements.txt
#   python fetch_models.py     # the two ONNX models, both ungated
#
# This replaced pyannote.audio, and with it PyTorch, torchaudio, torchcodec and a CUDA wheel
# chain — tens of megabytes instead of several gigabytes, no gated model, and no Hugging Face
# token needed before the first Diarize.
#
# The separate venv is kept: this and the STT service each pull their own ONNX Runtime build,
# and mixing them has caused version conflicts before.
sherpa-onnx>=1.13.5
numpy>=2.4.6
