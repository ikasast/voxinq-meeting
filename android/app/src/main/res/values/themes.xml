<?xml version="1.0" encoding="utf-8"?>
<!-- A light theme here and a dark one in values-night: the WebView's prefers-color-scheme
     follows the theme's isLightTheme, so this is also what lets the page follow dark mode. -->
<resources>
    <style name="Theme.Voxinq" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:colorPrimary">@color/accent</item>
    </style>

    <!-- The share question: a dialog over the app the person shared from, not a screen of
         Voxinq's own. -->
    <style name="Theme.Voxinq.Share" parent="android:Theme.Material.Light.Dialog.Alert">
        <item name="android:colorAccent">@color/accent</item>
    </style>
</resources>
